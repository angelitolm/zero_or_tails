# Install Zero or Tails

1. Clone repo:
```
yarn add --dev jest babel-jest @babel/preset-env @babel/preset-react 
yarn add --dev @testing-library/react @types/jest jest-environment-jsdom
```

2. Install:
```
yarn install
```

2. Run project:
```
cd zero_or_tails
yarn run dev
```